apiVersion: v1
kind: Service
metadata:
  name: airflow-web
  namespace: dev
  labels:
    app: airflow
    component: web
    release: airflow-dev
  annotations:
spec:
  selector:
    app: airflow
    component: web
    release: airflow-dev
    namespace: dev
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer